@using StaskoFy.ViewModels.Genre
@model IEnumerable<GenreIndexViewModel>

<div class="row g-3 justify-content-center mt-3">​
    <div class="col-12 col-md-6 col-lg-12 fs-5">​
        <h2 class="text-center">Genres:</h2>​
        <hr />
        <table class="table table-striped table-bordered table-responsive text-center">
                <thead>
                    <tr>
                        <td>Id</td>
                        <td>Name</td>
                        <td>Actions</td>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var genre in Model)
                    {
                        <tr>
                            <td>@genre.Id</td>
                            <td>@genre.Name</td>
                            <td>
                                <div class="d-flex justify-content-center gap-2">
                                    <a class="btn btn-warning btn-lg" asp-route-id="@genre.Id" asp-action="Edit">Edit</a>
                                    <form asp-action="Delete" method="post" asp-route-id="@genre.Id">
                                        <button class="btn btn-danger btn-lg">Delete</button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
          </table>
    </div>​
</div>